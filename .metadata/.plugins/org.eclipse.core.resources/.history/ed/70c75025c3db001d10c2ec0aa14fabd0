#include "kernel.h"

int main(){

	t_config* config = config_create("cfg/config.cfg");

	char* ip = config_get_string_value(config,"IP_MEMORIA");
	int puerto = config_get_int_value(config, "PUERTO_MEMORIA");

	conexionMemoria(ipMemoria, puertoMemoria);

}

void conexionMemoria(char ip, int puerto){

}
